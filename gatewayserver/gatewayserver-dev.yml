tracer:
    property: I AM GATEWAY SERVER DEV

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
    cloud:
        gateway:
            discovery:
                locator: #A
                    enabled: true 
                    lowerCaseServiceId: true

