tracer:
  property: I AM THE ORGANIZATION SERVICE DEFAULT

  
security:
  oauth2: 
    resource:
      userInfoUri: "http://localhost:8901/user"
  jpa:
    database: POSTGRESQL
    show-sql: true
  datasource:
    url: jdbc:postgresql://organizationdb:5432/eagle_eye_local
    platform: postgres
    username: postgres
    password: postgr@s
    driverClassName: org.postgresql.Driver
    tomcat:
      test-while-idle: true
      validation-query: SELECT 1